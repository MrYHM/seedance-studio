---
name: seedance-studio
description: Use when 用户想生成 Seedance/即梦视频提示词，把创意、故事或小说改编为短剧剧本，拆分分镜头脚本，规划多集 AI 短剧，生成角色/场景参考图提示词，或提到 seedance、即梦、分镜、shotlist、短剧、文生视频、视频提示词、storyboard 时使用。
---

# Seedance Studio：创意 → 剧本 → 分镜 → 提示词

## 概览

把一句话创意/故事/小说变成可直接粘贴到 Seedance（即梦）的分镜头脚本提示词，支持多集连载。三条核心原则：

1. **一致性交给参考图锁，提示词只写变化**——角色外观由资产图冻结，每镜绝不复述外观，复述会抢占注意力、弱化动作。
2. **把试错留在便宜的层**——剧本层随便改，分镜层确认后才编译提示词；每层过 gate 才往下走。
3. **镜头数是算出来的结果**——先排故事节拍→每个节拍选镜头类型→按类型给时长→加总校准。禁止用「总时长 ÷ 平均值」倒推。

## 五阶段流程

| 阶段 | 做什么 | Gate | 读什么 |
|---|---|---|---|
| 0 采访 | 先给「迷你方案」再让用户微调 | 用户认可方案 | references/01-interview.md |
| 1 剧本 | 核心梗→人物小传→分集大纲→△体剧本 | **用户确认分集大纲后才写正文** | references/02-script.md |
| 2 资产 | C/S/P 素材清单 + 参考图 T2I 提示词 | — | references/03-assets.md |
| 3 分镜 | 节拍表→镜头类型→shot list | **用户确认 shot list 后才编译** | references/04-storyboard.md |
| 4 编译 | 输出 8+1 区块提示词 + 自检 | check_prompt.py 通过 | references/05-prompt-v20.md 或 06-prompt-v25.md |
| 5 连载 | 尾帧五要素 + 抽帧接力 + 状态胶囊 | — | references/07-series.md |

**Fast Lane**：用户只要一条简单的单镜/单条视频（「来一条 15 秒的 XX」）时，跳过 Phase 1-2 的仪式：快速采访（≤3 问）→ 直接排节拍分镜 → 编译。不要对简单请求跑完整 gate 循环。多集、有剧情、有固定角色的请求必须走全流程。

## 硬约束（任何阶段不得违反）

1. **版本默认 Seedance 2.0**；用户点名 2.5 或需要 >15s 单条时切 2.5。两版模板不得混用（2.0 时间轴体 / 2.5 Shot 体）。
2. **声音行是字面常量**：`【声音】只生成音效和人声，不要生成任何背景音乐。不要生成任何字幕。`——即梦会自动配乐和烧字幕，后期无法剪除。
3. **时间码连续**：从 0 开始、无断档、无重叠，末镜结束时间 = 总时长。编译后必须跑 `scripts/check_prompt.py` 自检。
4. **每镜三要素**：≥1 个强动词、≥1 个运镜词、≥1 个环境/光线动态。缺一即静帧，重写。
5. **反 slop**：禁「8K、4K、超清、大师级、电影感、高级感、饱和度高」等空词；画质用具体物理表达（光线、材质、镜头感、颗粒），见 references/08-vocab.md 替换表。
6. **台词**：内联在所在镜头、带语气标注、每句 ≤15 字、语速 ≤6 字/秒（一镜能说多少字 = 镜头秒数 × 6）。
7. **内部人设不进提示词**：动机、潜台词、权力关系只写进剧本和状态胶囊；提示词里只出现可拍的行为（「指节攥白」而非「她很紧张」）。
8. **限制段只选 3-8 项**：先修正文矛盾，再写限制；不要用几十个否定词掩盖含糊的动作。
9. 冲突裁决顺序：平台硬限 > 用户明确要求 > 时间轴/一致性正确 > 摄影逻辑 > 风格 > 本 skill 默认值。

## 输出契约

最终提示词 = **8+1 区块**格式，模板见 `templates/分镜脚本模板.md`，黄金范例见 `examples/`。四类产物：剧本（`templates/剧本模板.md`）、素材清单（`templates/素材清单模板.md`）、分镜脚本提示词、状态胶囊（`templates/状态胶囊模板.md`，多集时）。

## 加载地图

| 情境 | 加载 |
|---|---|
| 挑规避词、查敏感词替换 | references/09-constraints.md |
| 查运镜/景别/光影/情绪写法 | references/08-vocab.md |
| 交付前自检 | references/10-qa.md + scripts/check_prompt.py |
| 用户给了参考图/视频素材 | 对应版本的 prompt 模板文件中「素材引用」节 |

按需加载，不要一次读完所有 references。
