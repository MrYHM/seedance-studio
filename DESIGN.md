# seedance-studio 设计文档

日期：2026-08-15
状态：已由用户确认（对话中逐项决策）

## 目标

一个 Claude Code 个人 skill：一句话创意/故事/小说 → 多集短剧剧本 → 资产参考图提示词 → 8+1 区块分镜头脚本提示词（Seedance 2.0/2.5 双版本）→ 集间衔接管理。纯文本产出，不调用生成 API。

## 用户已确认的关键决策

| 决策点 | 结论 |
|---|---|
| 目标版本 | 双版本自适应，**默认 Seedance 2.0**，采访时可切 2.5 |
| 工作流范围 | 剧本 + 分镜 + 参考图提示词 + 多集连载规划（全流程） |
| 题材定位 | 完全通用（不预设古风轻喜；肤质区块为真人剧可选模块） |
| 时长形态 | 多集连载为主；单集默认 15s（2.0）/ 支持 30s（2.5） |
| 架构 | 方案 B：单 skill + 分层 references + 阶段 gate + 校验脚本 |
| 画质词 | 采纳反 slop 规则：删「超清8k/饱和度高」类空词，用具体物理描述 |
| 集间衔接 | 抽帧接力为主（尾帧截图作下集首帧参考），延长法为备选 |

## 输出契约：8+1 区块分镜头脚本（本 skill 的黄金格式）

源自用户实战验证的模板，修订点：①区块去 slop；④人物拆「内部人设/可拍载体」两层；⑥时间码强制连续；新增⑨限制段。

① 风格与画质（含负向约束：字幕透明度0%、无音乐留人声）／② 时间／③ 场景（引用 S 资产短名）／④ 人物（内部人设不进提示词，只出可拍行为）／⑤ 故事内容（标注本集在节奏地图中的位置）／⑥ 分镜时间轴（时间码连续；每镜【景别+运镜】+强动词动作链+环境动态；台词内联带语气、≤15字/句、≤6字/秒）／⑦ 光影／⑧ 画质肤质（真人剧可选）／⑨ 限制（按题材挑 3-8 项）

## 五阶段工作流

Phase 0 采访（迷你方案先行）→ Phase 1 剧本（gate：分集大纲确认）→ Phase 2 资产（C/S/P + T2I 提示词）→ Phase 3 分镜（gate：shot list 确认）→ Phase 4 编译（8+1 区块 + check_prompt.py 自检）→ Phase 5 连载（尾帧五要素 + 抽帧接力 + 状态胶囊）。
Fast Lane：单条简单需求跳过 Phase 1-2 仪式。

## 借鉴来源图谱

- Emily2040/seedance-2.0：Fast Lane、渐进加载、导演读内部字段、反 slop、压缩优先级、状态胶囊
- liangdabiao/Seedance2-Storyboard-Generator：六步改编法、幕结构自适应、△体剧本、C/S/P 编号、尾帧五要素、抽帧接力
- A-cat-with-carrots/OnlyShot：阶段 gate、节奏地图按秒爆点、敏感词替换表、对白 7 原则、时长变奏
- huangbai-AI/sd-2-5-prompt：限制段方法论（3-8项、先修矛盾）、规避词库、动作因果链、check_prompt.py 原型、台词语速系数
- woyin2024/lengyi-seedance2.5-prompt：资产锚定、镜头数=算出来的、情绪可见写法、每镜三要素、自报字数

## 明确不做

- 不调用即梦 CLI / 任何生成 API（OnlyShot 的 scripts 层不移植）
- 不做多语言 vocab、不做 provenance 账本、不做 28 子 skill 拆分（Emily2040 过度工程）
- 数值口径全库统一：特写 2-3s / 中景 3-5s / 全景 5-7s（消除 lengyi 三处不自洽）
